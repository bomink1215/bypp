@AGENTS.md

# BYPP

위치를 기반으로 **오늘 먹을 메뉴를 추천**하고 그 메뉴를 파는 주변 음식점을 찾아주는 웹 서비스.

혼자 쓰는 흐름과 **여럿이 같이 고르는 흐름**이 있다. 후자가 이 서비스의 무게중심이다 —
"뭐 먹지"의 진짜 어려운 문제는 혼자 못 정하는 게 아니라 N명이 합의를 못 하는 것이다.

## 데이터 제약 — 재조사하지 말 것

이 프로젝트의 진짜 제약은 프레임워크가 아니라 **데이터의 부재**다. 아래는 전부 확인된 사실이다.

- **메뉴판 데이터가 없다.** 카카오/네이버/구글 어떤 공개 API도 가게의 메뉴를 제공하지 않는다.
- **영업시간 데이터도 없다.** 카카오 로컬 API 응답은 `id / place_name / category_name / category_group_code / phone / address_name / road_address_name / x / y / place_url / distance`가 전부다. "장소 상세정보(영업시간·리뷰)를 불러오는 API는 제공하지 않는다"가 카카오 공식 답변이다.
- 무료 대체 소스를 전부 확인했고 전부 탈락했다.
  - **Google Places** `regularOpeningHours` → Enterprise SKU. 무료 월 1,000콜(≈검색 66회), 결제수단 등록 필수.
  - **OSM Overpass** → 강남역 일대 음식점·카페 **1,531곳 중 35곳(2.3%)** 만 `opening_hours` 보유. 실측값.
  - **공공데이터 전국일반음식점표준데이터** → 인허가 정보라 영업시간 컬럼 자체가 없고, 오픈API도 아닌 CSV 다운로드.
  - **포털 크롤링 / 카카오 비공개 내부 엔드포인트** → 약관 위반 소지. 채택하지 않는다.

### 카카오 검색의 45건 노출 상한 (중요)

카카오는 검색당 **최대 45건만 노출한다**(`meta.pageable_count` 상한). 밀집 지역에서는
이게 조용히 결과를 망친다.

```
강남역 반경 772m:  total_count 1428  →  pageable_count 45
sort=distance로 받은 45건의 가장 먼 거리: 119m
```

즉 772m를 훑은 게 아니라 **119m를 훑은 것**이다. 이 표본으로 "근처에 국밥집이 없다"고
판정하면 143m에 있는 국밥집을 놓친다. 실제로 그렇게 놓쳤었다.

- 카테고리 검색 결과는 `complete`(= `총 개수 <= 받은 개수`)일 때만 "없다"의 근거가 된다.
  표본일 때는 존재 판정도 밀도 가중치도 쓰지 않는다.
- **키워드 검색은 이 상한에 걸리지 않는다.** 질의가 결과를 미리 좁히기 때문이다.
  그래서 밀집 지역의 존재 확인은 키워드 검색이 맡는다.

### 카카오 키워드 검색은 느슨하게 매칭한다

"해장국"으로 검색하면 `분식 > 명인만두`가, "짬뽕"으로 검색하면 베트남 쌀국수집이 섞여
나온다. 그대로 보여주면 추천한 메뉴를 안 파는 가게를 들이미는 셈이다.
`src/lib/kakao/relevance.ts`가 **업종이 맞거나 상호에 메뉴명이 들어간 것만** 남긴다.
남는 게 0건이면 그 메뉴는 사용자에게 보이지 않고 다음 후보로 넘어간다.

## 설계 원칙

기능 제안이 이 셋과 충돌하면, 제안 쪽이 먼저 반박되어야 한다.

### 원칙 1 — 추천의 세밀도가 검증의 세밀도를 넘으면 안 된다

카카오가 주는 건 `음식점 > 한식 > 국밥`까지다. 그 집이 순대국밥을 파는지 고기국밥을 파는지는 알 수 없다. 그래서 추천도 **"국밥"까지만** 한다. "고기국밥"을 추천하면 근처 국밥집이 순대국밥만 팔 때 약속을 어기게 된다. **추천이 약속하지 않은 것은 어긋날 수 없다.**

시드 편입 기준은 취향이 아니라 테스트 가능한 규칙이다 — *그 이름으로 카카오 키워드 검색을 했을 때 가게가 실제로 잡히는 수준까지만 내려간다.* `npm run verify:seed`가 이걸 강제한다. 시드를 늘리면 반드시 돌릴 것.

### 원칙 2 — 시간대 라벨은 메뉴가 아니라 업종에 붙인다

"파스타는 야식인가"는 취향 논쟁이라 답이 없다. "이탈리안 음식점이 새벽 2시에 여는가"는 업종의 사실 문제고 카카오맵에서 몇 곳 눌러보면 검증된다. 라벨 대상도 메뉴 50~100개가 아니라 업종 20~30개로 준다.

그래서 **`Menu` 타입에 `mealTimes` 필드가 없다. 이건 누락이 아니라 결정이다.** 시간대는 `src/lib/menu/hours.ts`가 `Cuisine` 단위로 갖고 있고, **하드 필터가 아니라 순위 가중치로만** 쓴다. 추정이 틀려도 사용자 선택지를 강제로 지우지 않기 위해서다.

### 원칙 3 — 제약과 선호는 다른 종류의 정보다

"매운 거 먹고 싶다"(선호)와 "매운 거 못 먹는다"(제약)는 섞으면 안 된다. 앞은 협상 가능한
바람이고 뒤는 체질·알레르기·신념이다. 그래서 다루는 법이 정반대다.

|  | 합치는 법 | 완화 | 저장 |
|---|---|---|---|
| 제약(`못 먹는 것`) | **합집합** — 한 명이라도 못 먹으면 전체 제외 | **절대 안 함** | 저장됨 |
| 선호(맛·토글) | 타협 | 필요하면 품 | 매번 새로 |

이렇게 나누면 "A는 매운 걸 원하고 B는 매운 걸 못 먹는다"에서 누구를 희생시킬지 계산할
필요가 없다. B의 제약이 이기고 A의 선호는 남은 후보 안에서 작동한다.

**제약은 후보가 0개가 되어도 풀지 않는다.** 다른 조건을 풀고, 그래도 없으면 없다고 말한다.
못 먹는 걸 추천하느니 빈손이 낫다.

다만 **이건 알레르기 안전 필터가 아니다.** 메뉴판 데이터가 없어 특정 가게의 조리법을 모르고,
시드에 붙인 대분류 태그일 뿐이다. UI 문구는 "안전"이 아니라 "이런 건 빼고 추천"에 머물러야
하고 최종 확인은 가게에서 하도록 안내한다(원칙 1과 같은 얘기다).

로그인이 없으면 `localStorage`뿐이라 기기를 바꾸면 초기화된다. 제약이 조용히 사라지면
위험하므로 **결과 화면에 "무엇을 빼고 있는지"를 항상 띄운다.** 그 줄이 없어진 걸 봐야
다시 설정할 수 있다.

### 원칙 4 — 학습이 탐색을 죽이면 안 된다

피드백을 그대로 반영하면 몇 번 만에 같은 메뉴 서너 개만 돈다. 그래서 선호 가중치는 **0.3~2.5로 clamp**하고(싫어요를 눌러도 사라지지 않는다), **15% 확률로 취향을 통째로 무시하며**(ε-greedy), 오래된 피드백은 **반감기 30일로 감쇠**시킨다. 취향은 변한다.

## 추천 흐름

```
1. 입력       좌표 + 시각 + 도보 분 + 특성 필터
2. [서버]     카카오 category 검색 (FD6, x/y/radius)
              → 반경 안에 실제로 존재하는 업종 집합
3. [서버]     category_name → cuisine 매핑 → 주변에 가게가 있는 메뉴만 후보로
              → 특성 하드 필터 + 시간대 가중치로 기본 점수
              ← 후보 목록 반환                        (카카오 호출 1회)
4. [클라]     로컬 취향 기록을 곱해 가중 랜덤으로 1개 선택
5. [서버]     그 메뉴명으로 카카오 keyword 검색 → 0건이면 다음 후보로 (최대 3회)
6. 표시       메뉴 + 지도 + 가게 목록 + 피드백 버튼
7. 피드백     좋아요 → 확정 / 다른 거 → 4번으로
```

**"추천했는데 근처에 파는 가게가 없다"를 막는 게 이 흐름의 목적이다.** 막는 지점은 두 곳이다.
한산한 지역에서는 2~3번의 카테고리 스윕이 전수 조회라 후보 단계에서 걸러지고, 밀집 지역에서는
스윕이 표본일 뿐이라(위 45건 상한) 5번의 키워드 검색이 실제 확인을 맡는다.
**어느 쪽이든 가게가 1곳 이상 잡힌 메뉴만 화면에 뜬다.** 이 보장을 없애는 제안은
그 문제를 어떻게 막을지부터 답해야 한다.

**서버/클라이언트를 3번에서 자른 이유**는 두 가지다. 취향 이력이 기기 밖으로 나가지 않고, "다른 거"가 후보 목록을 재사용해 **카카오 재호출 없이** 즉시 응답한다.

## 사용자 옵션

맛만 소프트고 나머지는 하드 필터다. 못 먹는 것·위치·시간 아래는 **"조건 더 보기"로 접어둔다** —
"조작할 게 많다"와 "기준을 늘려 달라"는 피드백이 같이 들어와서, 기준은 늘리되 기본 화면에서는
치웠다. 방에서는 조건을 내는 게 목적이라 펼친 채로 시작한다.

| 그룹 | 항목 | 방식 |
|---|---|---|
| 밥·면 | 밥 / 면 / 그 외 | 하드. "밥 먹을까 면 먹을까"가 가장 먼저 하는 고민이라 맨 위 |
| 맛 | 순함↔매움, 담백함↔느끼함, 시원함↔뜨끈함 | 슬라이더. **목표값이지 필터가 아니다** |
| 식재료 | 고기(+종류: 돼지·소·닭) / 해물 / 밀가루 / 달걀 / 유제품 | 있음·없음·상관없음 (하드) |
| 양 | 가볍게 / 든든하게 | 하드 |
| 가격대 | 1만원 이하 / 1~2만원 / 2만원 이상 (1인) | 하드. **메뉴의 보통 가격대지 가게 가격이 아니다** |
| 기타 | 국물 / 혼밥 / 빨리 먹기 / 격식 | 있음·없음·상관없음 (하드) |

맛 슬라이더는 **다섯 칸이고 가운데 칸이 상관없음(null)** 이다. 맛 값은 0~3 네 단계인데
네 칸짜리 바에는 정중앙이 없어 기본 상태가 한쪽으로 치우친다. 양극단 바에서 가운데가
"어느 쪽도 아님"인 건 의미상으로도 맞다. 목표에서 한 칸 멀어질 때마다 0.2배가 되어
사실상 필터처럼 걸러내되 0은 아니다(원칙 3).

**식재료를 완화 순서의 맨 뒤에 두는 건 의도다.** 알레르기나 채식일 수 있어 어기면
아쉬운 정도가 아니라 못 먹는 걸 추천하는 셈이 된다. 완화 순서는
`quick → solo → weight → soup → staple → price → formal → meatKind → dairy → egg → flour → seafood → meat`.
**밥·면은 조리 방식(구이·튀김…) 대신 고른 축이다.** 조리 방식으로 나누면 튀김·찜이 3개씩이라
거의 메뉴를 직접 고르는 셈이 되고 어디에도 안 드는 메뉴가 8개였다. 밥·면·그 외는 20/12/19로
고르게 나뉘고 논쟁 여지가 적다. 취향 학습 축에도 넣어 면 요리 선호가 다른 면 요리로 전이된다.

가격대는 격식 바로 앞이다 — 대접 자리에서 격식을 버리는 것보다 예산을 조금 넘기는 게 낫다.
격식은 편의 조건보다 뒤다 — 윗사람 대접 자리에 분식이 뜨는 건 아쉬운 정도가 아니다.

**고기 종류는 `meat`의 세부다.** 대표 구성 기준이고 둘 다 흔하면 둘 다 true(곱창·갈비). 양꼬치처럼
셋에 안 드는 고기는 `meat`만 true다. 못 먹는 것에는 "고기 전체"(채식)와 돼지·소·닭(종교·체질)을
둘 다 둔다 — 돼지만 못 먹는 사람에게 "고기"로 뭉치면 먹을 수 있는 걸 너무 많이 뺀다.
`verify:seed`가 "종류가 있는데 meat가 false"인 메뉴를 막는다.

**격식(`formal`)·가격대(`price`)는 메뉴에 붙이고 가게는 약속하지 않는다.** 카카오는 가게 분위기를 주지 않으므로
"대접할 만한 메뉴"(한정식·갈비·스테이크·초밥·회 등)까지만 고르고, 분위기와 룸 여부는
카카오맵에서 확인하라고 안내한다(원칙 1). 취향 학습에도 넣지 않는다 — 자리의 성격이지
그 사람의 입맛이 아니기 때문이다.

## 피드백은 두 버튼이다

| 버튼 | 반영 |
|---|---|
| 좋아요 | 지속 가점 + 특성 축 전이. 추천 확정 |
| 다른 거 | 당일 후보에서 제외 + 지속 감점(0.4배) + 재추천 |

"다른 거"의 감점이 1이 아니라 **0.4**인 이유: 이 버튼은 "이 메뉴가 싫다"와 "지금은 안
당긴다"가 섞인 신호다. 오늘 점심에 국밥이 안 당긴 것을 국밥을 싫어하는 것과 똑같이
취급하면 한두 번 넘긴 메뉴가 영영 사라진다. 그래서 당일 후보에서는 바로 빼되(확실한
의사) 장기 취향에는 약하게만 반영한다. 반복해서 넘기면 쌓여서 제대로 내려간다.

학습은 두 층위다. **메뉴 단위**(그 메뉴의 누적 평가)와 **특성 단위**(cuisine / 맛 3축 /
식재료 3종 / 국물 / 양으로 전이). 특성 전이가 없으면 시드 60여 개를 다 눌러봐야 학습이 된다.

**특성 점수는 일치한 축만 골라 평균 내면 안 된다.** 전체 축 수로 나눠야 한다. 안 그러면
축 하나만 겹치는 메뉴가 일곱 개 겹치는 메뉴와 같은 점수를 받는다(마라탕에 좋아요를
눌렀더니 샐러드가 짬뽕만큼 오르던 버그).

**"좋아요"는 확정이기도 해서 먹은 기록(`eaten`)을 따로 남긴다.** 취향으로만 반영하면 어제
점심에 확정한 국밥이 오늘 점심에 **더** 잘 나온다. 그래서 먹은 메뉴는 0.2배에서 시작해 사흘에
걸쳐 1로 돌아오는 배수(`recencyFactor`)를 따로 곱한다. 취향 탐색(ε) 중에도 적용한다.
방에서 정해진 메뉴도 먹은 걸로 남기되 취향 카운트는 올리지 않는다 — 내가 고른 게 아닐 수 있다.
먹은 기록은 계정으로 동기화하지 않는다. 취향이 아니라 최근 사정이다.

## 후기와 나의 먹로그

카카오는 별점을 주지 않는다. 그래서 **우리가 직접 모은다.** 가게 카드의 "다녀왔어요 · 후기 쓰기"로
별점(1~5)과 글(300자)을 남기면 두 곳에 쓰인다.

| | 보이는 곳 | 담기는 것 |
|---|---|---|
| 공개 | 가게 목록 "이 앱 평점" + 후기 보기 | 별점, 글, 작성자가 고른 닉네임(비우면 익명) |
| 본인만 | 나의 먹로그(`/mokrog`) 카드 | 위 전부 + 먹은 메뉴 + **함께한 사람** |

- **별점은 가게에 준다.** 먹은 메뉴는 카드에 기록으로만 남는다.
- **함께한 사람은 공개 응답에 절대 넣지 않는다.** 방에서 정한 한 끼면 서버가 방 참가자 닉네임으로
  채운다 — 클라이언트가 보낸 이름은 믿지 않는다.
- **로그인 없이도 쓴다**(원칙: 로그인은 선택). 작성자는 방과 같은 브라우저 토큰이고, 로그인하면
  `claim`으로 계정에 묶여 다른 기기에서도 보인다. user id는 클라이언트 값이 아니라 액세스 토큰을
  Supabase에 되물어 얻는다(`src/lib/supabase/auth.ts`).
- 같은 사람이 같은 가게에 **하루 한 번**만 쓸 수 있다(유니크 인덱스). 브라우저를 바꾸면 우회되는
  최소한의 장치라, 평점 조작이 실제로 보이면 로그인 필수·신고 기능을 검토한다.
- 목록에는 "이 앱 평점"과 **개수**를 같이 띄운다. 한두 개짜리 평균을 카카오 별점처럼 보이게 하지 않는다.
- 브라우저 토큰은 PostgREST `or=(…)` 필터에 들어가므로 `crypto.randomUUID()` 글자만 받는다.
- 배지는 저장하지 않고 기록에서 매번 계산한다(`src/lib/review/badges.ts`). 기준을 바꿔도 옛 데이터를
  고칠 필요가 없다.

## 같이 고르기 (방)

```
방장이 만나는 장소·시각·도보 시간을 정해 방 생성 (6자리 코드)
  → 링크·코드 공유 → 닉네임 입력 후 입장
  → 각자 조건 제출 (3초 폴링으로 진행 현황 표시)
  → 방장이 후보 뽑기 → 5개 중 투표 → 모두 투표하면 자동 마감 → 메뉴 확정 알림
  → (선택) "가게도 정할까요?" → 가게 5곳 중 투표 → 자동 마감 → 가게 확정 알림
```

- **모두 투표하면 서버가 자동으로 마감한다.** 방장만 마감할 수 있으면 방장이 자리를 비울 때
  방이 멈춘다. 방장은 안 오는 사람을 기다리지 않도록 수동 마감도 할 수 있다. 마지막 두 표가
  동시에 들어와도 한 번만 마감되도록 `status=eq.voting` 조건을 건 채 갱신한다.
- **가게 투표는 누구나 열 수 있다.** 정해진 메뉴를 파는 가게(`filterRelevant` 통과)를 가까운 순
  5곳으로 스냅샷한다. 가게를 안 정하고 끝내도 된다 — 방은 `decided`에 머문다.
- **정해지는 순간을 알린다.** 자동 마감이라 마지막 표를 낸 사람 말고는 언제 정해졌는지 모른다.
  폴링으로 전환을 알아채면 화면에 확인 창을, 다른 탭이면 브라우저 알림을 띄운다. 권한은 투표
  버튼을 누를 때 묻는다. 처음 불러왔을 때 이미 정해져 있으면 띄우지 않는다. 브라우저를 닫은
  사람에게는 안 간다(서비스 워커·푸시 서버가 필요해서 하지 않았다).
- **공유 버튼은 조건을 모으는 동안만 보인다.** 입장이 그때만 되기 때문이다.

- **위치는 방장이 정한다.** 참가자에게 위치 권한을 묻지 않아 마찰이 없다. 지도에서 직접
  찍을 수 있다(`LocationPicker` — 마커를 끄는 대신 지도를 움직이고 화면 중앙 고정 핀이
  가리키는 곳을 고른다. 손가락이 핀을 가리지 않아 모바일에서 정확하다). 노트북에서 끌어서
  찾기 불편하다는 피드백으로 **이름 검색창**을 붙였다 — 결과를 누르면 지도만 옮기고 확정은 핀으로 한다.
- **로그인은 없다.** 브라우저가 만든 토큰이 신원이고 닉네임으로 서로를 알아본다.
  응답에 남의 토큰을 절대 넣지 않는다.
- **후보는 스냅샷으로 저장한다.** 투표 중에 후보가 바뀌면 이미 던진 표의 의미가 사라진다.
- 동기화는 **3초 폴링**이다. Realtime을 쓰려면 브라우저가 Supabase에 직접 붙어야 해서
  publishable 키 노출과 RLS 정책 설계가 따라온다. 3초면 체감상 실시간과 거의 같다.

### 여럿의 조건을 합치는 법

제약은 합집합(원칙 3). 나머지는 **가장 불만인 사람의 만족도를 최대화**한다 — 평균이면
한 명이 크게 싫어하는 메뉴가 "나머지가 좋아하니까" 뽑힌다. 매움 3과 순함 0이 부딪히면
양 끝이 아니라 중간 매운맛이 이긴다. (실측: 매움3·순함0·국물·고기없음이 섞인 3인방에서
후보 5개가 전부 매운맛 1로 나왔다.)

**토글(고기·해물·국물·혼밥·양 등)을 교집합으로 걸러내면 안 된다.** 세 명이 각자 하나씩만
걸어도 후보가 붕괴한다 — 실제로 `국물 있음` + `고기 없음` 두 개에 50개가 1개가 됐다.
투표할 게 없으면 같이 고르는 의미가 없다. 그래서 어긋나면 점수만 불리하게 하고 지우지
않는다(`groupScorer`). 진짜 못 먹는 것은 제약이 따로 걸러낸다.

서로 반대되는 토글(A는 고기 있어야, B는 없어야)은 **해제하고 이름을 표시한다.**
조용히 넘어가면 사용자는 자기 조건이 지켜진 줄 안다.

## 디자인

색은 로고에서 뽑았고 `src/app/globals.css`의 `@theme`에 있다.

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `cream` | `#FFF6EC` | 페이지 바탕 (로고 배경과 같은 색) |
| `brand` | `#E8553B` | **화면의 주 행동 버튼**, 강조 라벨, 내가 고른 것(투표·가게) |
| `neutral-*` | 따뜻한 갈색 계열 | 나머지 전부. `neutral-900`(`#2A1E17`)이 로고 글자색 |

- **Tailwind의 `neutral` 팔레트를 통째로 갈색으로 덮어썼다.** 컴포넌트는 그냥 `neutral-*`을 쓰면
  된다. 차가운 회색(`gray`/`zinc`/`slate`)을 새로 쓰지 않는다 — 크림 바탕에서 떠 보인다.
- **코랄은 아껴 쓴다.** 주 행동 하나 + 강조 몇 곳. 칩의 "선택됨"은 코랄이 아니라 잉크(`neutral-900`)다.
  전부 코랄이면 어디를 눌러야 할지 안 보인다.
- 카카오 노란 버튼(`#FEE500`)은 카카오 가이드라서 브랜드색으로 바꾸지 않는다.
- **다크 모드는 없다.** 로고가 크림 바탕 위에서 완결된 디자인이라 어두운 바탕에서 워드마크 글자가
  묻힌다. `dark:` 클래스를 쓰지 않는다.
- 슬라이더는 브라우저 기본 모양 대신 `globals.css`에서 직접 그린다. 손잡이 색은 `text-*`로 준다.

로고 파일:

| 파일 | 용도 |
|---|---|
| `public/logo.png` | 워드마크(태그라인 포함, 투명 배경). 첫 화면 |
| `public/logo-mark.png` | 핀 마크만. 헤더(`Brand`), 지도 중앙 핀 |
| `src/app/icon.png` / `apple-icon.png` | 파비콘 / iOS 홈 화면. Next가 파일명으로 자동 등록 |
| `src/app/opengraph-image.png` | 링크 미리보기. 방 링크를 카톡으로 공유할 때 뜬다 |

## 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js 16 (App Router) + TypeScript | API 키를 숨길 서버 라우트가 프론트와 한 프로젝트에 존재 |
| 스타일 | Tailwind CSS v4 | 모바일 우선 UI를 빠르게 |
| 장소 검색 | 카카오 로컬 API (REST) | 좌표+반경 검색, `FD6`/`CE7`, **일 10만 호출** 무료 |
| 지도 | 카카오 Maps JS SDK | 로컬 API와 동일 생태계 |
| DB | Supabase(Postgres) | 방·참가자·투표는 **서버 라우트에서 secret 키로만**. 로그인 사용자의 취향(`user_profiles`)만 브라우저가 RLS 아래 직접 |
| 로그인 | Supabase Auth + 카카오 OIDC | 선택 사항. 취향을 기기 간에 유지하는 용도뿐 |
| 저장소 | `localStorage` | 취향 기록과 못 먹는 것. 게스트는 **기기 간 공유가 안 된다**(로그인하면 계정과 동기화) |
| 배포 | Vercel | HTTPS 자동 제공(Geolocation 필수 조건) |

검토 후 제외: **네이버 지역검색 API**(한 번에 5건), **Google Places**(결제수단 필수, 국내 POI 품질), **Vite + React**(키를 가릴 백엔드 별도 필요).

## 환경변수 규칙 (중요)

```
KAKAO_REST_API_KEY=                    # 서버 전용 (단, 로그인 인가 URL의 client_id로는 노출됨 — 아래 참조)
KAKAO_CLIENT_SECRET=                   # 서버 전용. 로그인 토큰 교환에 필요
NEXT_PUBLIC_KAKAO_JS_KEY=              # 브라우저 노출됨
SUPABASE_URL=                          # 서버 전용
SUPABASE_SECRET_KEY=                   # 서버 전용. RLS 우회
NEXT_PUBLIC_SUPABASE_URL=              # 브라우저 노출됨
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=  # 브라우저 노출됨. RLS의 제약을 받는다
```

- `NEXT_PUBLIC_` 접두사가 붙은 값은 **브라우저 번들에 그대로 박힌다.**
- JS 키는 카카오 콘솔에서 도메인 제한이 걸리므로 노출돼도 된다. 그 키의 **JavaScript SDK 도메인**에 `http://localhost:3000`을 등록해야 한다(**뒤에 슬래시 없이** — 슬래시가 붙으면 "유효하지 않은 URL Host" 오류다). 도메인은 키마다 따로 관리된다.
- **REST 키에는 절대 `NEXT_PUBLIC_`을 붙이지 않는다.** REST 키에는 Referer 제한이 없어서 새어나가면 누구나 쿼터를 태울 수 있다. 카카오 호출은 전부 서버 라우트에서 하고, `src/lib/kakao/client.ts`가 `server-only`를 import해 클라이언트 번들 유입을 **빌드 타임에** 막는다.

## Supabase

스키마는 `supabase/migrations/`에 있다. **PostgREST는 DDL을 지원하지 않으므로 대시보드
SQL Editor에서 직접 실행해야 한다.** 테이블이 없으면 `PGRST205`가 뜬다.

- `0001_rooms.sql` — 방·참가자·투표
- `0002_user_profiles.sql` — 로그인 사용자의 취향
- `0004_reviews.sql` — 후기(자체 평가)와 먹로그. 0003과 같이 **코드 배포 전에 먼저 실행한다**
- `0003_place_voting.sql` — 가게 투표. **코드 배포 전에 먼저 실행한다**(컬럼·테이블 추가뿐이라
  옛 코드는 영향이 없고, 새 코드는 이게 없으면 가게 투표에서 실패한다)

`user_profiles`는 RLS 정책을 제대로 건다. 로그인한 사용자는 `auth.uid()`가 있어 "본인 것만"을
표현할 수 있기 때문이다. 그래서 이 테이블만은 브라우저가 publishable 키로 직접 읽고 쓴다.

반면 `rooms` / `participants` / `votes` 셋은 **RLS를 켜되 정책을 하나도 두지 않는다.** 참가자가
익명 토큰이라 "본인 것만"을 RLS로 표현할 수 없기 때문이다. secret 키(service_role)는 RLS를
우회하므로 서버는 정상 동작하고, publishable 키가 새어나가도 이 테이블들은 아무것도 읽히지
않는다.

Supabase 접근은 두 갈래로 나뉜다.

| | 파일 | 방식 | 키 |
|---|---|---|---|
| 서버 (방) | `src/lib/supabase/client.ts` | 내장 fetch로 PostgREST 직접 호출 | secret. `server-only` |
| 브라우저 (로그인·내 프로필) | `src/lib/supabase/browser.ts` | `@supabase/supabase-js` | publishable |

서버 쪽은 CRUD 몇 개뿐이라 라이브러리 없이 fetch로 충분하다. 브라우저 쪽에만
`supabase-js`를 쓰는 이유는 세션 저장과 토큰 갱신을 직접 구현할 일이 아니기 때문이다.
**서버 코드에 `supabase-js`를 끌어오지 않는다** — 방 데이터는 계속 fetch 래퍼로 간다.

**`SUPABASE_SECRET_KEY`는 RLS를 통째로 우회한다.** 카카오 REST 키보다 위험하다.
`server-only`로 클라이언트 번들 유입을 빌드 타임에 막고 있다.

## Next 16에서 바뀐 것

`middleware.ts`는 **폐기되고 `proxy.ts`로 이름이 바뀌었다**(export 이름도 `proxy`).
Supabase 공식 문서는 아직 `middleware.ts`로 안내하므로 그대로 따르면 폐기된 규약을 쓰게 된다.

`RouteContext<'/api/...'>` 같은 타입은 `next dev` / `next build` / `next typegen`이 생성한다.
새 라우트를 만든 직후 타입 오류가 나면 `npx next typegen`을 먼저 돌린다.

## 카카오 콘솔 사전 설정

카카오는 기능마다 콘솔 스위치가 따로 있고 **기본값이 꺼짐**이다. 키가 멀쩡한데 안 되면
대개 이쪽이다.

- `403 NotAuthorizedError: disabled OPEN_MAP_AND_LOCAL service` → **카카오맵 → 사용 설정 ON**
  (2024-12-01부터 기본 OFF)
- `KOE205` → 요청한 scope가 **카카오 로그인 → 동의항목**에 없음. 우리는 `profile_nickname`만
  요청하므로 그것만 켜져 있으면 된다 (아래 참조)
- 로그인 콜백에서 `id_token이 없습니다` → **OpenID Connect 활성화** 꺼짐

## 카카오 로그인

**카카오 OIDC로 `id_token`을 직접 받아 Supabase `signInWithIdToken`에 넘긴다.**
Supabase의 `signInWithOAuth({ provider: 'kakao' })`는 쓰지 않는다. 앞부분(토큰 받아오기)만
우리가 하고, 세션 관리는 여전히 Supabase가 한다.

```
AuthBar 로그인 버튼 → useAuth.signIn(next)
  → GET /api/auth/kakao/start        state·next를 httpOnly 쿠키에 심고 카카오 인가 화면으로 302
                                     scope = "openid profile_nickname"
  → kauth.kakao.com (사용자 동의)
  → GET /api/auth/kakao/callback     state 대조 → 코드를 id_token으로 교환 (client_secret, 서버에서)
                                     → next#kakao_id_token=… 로 302
  → useAuth가 프래그먼트를 읽고 즉시 지운 뒤 signInWithIdToken({ provider: 'kakao' })
  → onAuthStateChange(SIGNED_IN) → 로컬·계정 취향 병합
```

- **id_token은 URL 프래그먼트(`#`)로 넘긴다.** 프래그먼트는 서버로 전송되지 않아 로그·리퍼러에
  남지 않는다. 쓰자마자 `history.replaceState`로 지운다.
- **`state`는 CSRF 방어다.** 우리가 시작하지 않은 콜백은 쿠키와 대조해 걸러낸다.
- **돌아갈 주소(`next`)는 쿠키에 둔다.** URL로 실어 보내면 조작될 수 있다. `/`로 시작하고
  `//`로 시작하지 않는 같은 사이트 경로만 받는다(오픈 리다이렉트 방지).
- 실패하면 `?login_error=사유`로 돌아오고, `useAuth`가 꺼내 보여준 뒤 지운다.
  로그인 실패가 앱을 막지 않는다 — 게스트 상태로 계속 쓴다.

### 왜 Supabase의 카카오 OAuth를 안 쓰나

**Supabase는 scope에 `account_email`을 하드코딩해 보내고, 우리가 넘긴 `scopes`는 덧붙기만
한다.** 실측:

```
scopes 미지정           → "account_email profile_image profile_nickname"
scopes=profile_nickname → "account_email profile_image profile_nickname profile_nickname"
```

`account_email`은 개인 개발자 앱에 기본 제공되지 않아(비즈 앱 전환 + 승인 필요) `KOE205`로
거절당한다. 우리는 이메일이 필요하지도 않다. 그래서 인가 요청을 직접 만들어 닉네임만 요청한다.

### 콘솔 설정

카카오 developers:

- **카카오 로그인 → 사용 설정 ON**
- **OpenID Connect 활성화 ON** — 꺼져 있으면 토큰 응답에 `id_token`이 없다
- **Redirect URI**: `<origin>/api/auth/kakao/callback`. 로컬은 `http://localhost:3000/api/auth/kakao/callback`,
  배포 도메인도 따로 등록한다. (Supabase의 `/auth/v1/callback`이 아니다 — 콜백을 우리가 받는다)
- **동의항목**: `profile_nickname`. (`openid`는 동의항목이 아니라 OIDC 스위치로 켜진다)
- **Client Secret**: 앱 → 플랫폼 키 → REST API 키 → 클라이언트 시크릿. 새로 만든 REST 키는
  이미 활성화된 상태로 추가되고, 켜져 있으면 토큰 교환에 필수다. **`KAKAO_CLIENT_SECRET`에 넣는다.**

Supabase 대시보드:

- **Authentication → Providers → Kakao 활성화.** `signInWithIdToken`도 이 provider 설정으로
  토큰을 검증한다. Client ID는 카카오 REST API 키.
- **"이메일 없는 사용자 허용" ON** — 이메일을 요청하지 않으므로 id_token에 이메일이 없다.

우리 코드는 닉네임만 읽고, 그것도 화면 표시용이라 따로 저장하지 않는다.

### REST 키가 브라우저에 노출된다 (감수한 위험)

OAuth 규격상 `client_id`는 공개 값이라 인가 요청 URL에 그대로 실린다. 인가 URL을 서버
(`/api/auth/kakao/start`)에서 만들어도 브라우저가 그 URL로 이동하므로 가려지지 않는다.

```
kauth.kakao.com/oauth/authorize?client_id=<REST API 키>...
```

문제는 **그 키가 로컬 API 키와 같은 키**라는 점이다. 로컬 API는 `Authorization: KakaoAK <키>`
하나로 인증되고 Referer 제한이 없어서, 로그인 버튼을 눌러본 사람은 누구나 그 키로 우리
쿼터(일 10만)를 쓸 수 있다. **"REST 키는 서버에만 둔다"는 원칙이 카카오 로그인을 붙이는
순간 구조적으로 깨진다 — 카카오 설계상 피할 수 없다.**

개인 프로젝트 규모에서 일 10만이 소진될 일은 거의 없어 지금은 감수한다. 쿼터 남용이 실제로
관측되면 **로그인용 앱과 지도·검색용 앱을 카카오에서 분리**하는 것이 해법이다(앱을 두 개
관리해야 하므로 필요해지기 전에는 하지 않는다).

### `@supabase/ssr`과 proxy를 쓰지 않는다

로그인으로 하려는 건 "내 취향을 기기 간에 유지"뿐이고, 그 데이터(`user_profiles`)는 RLS로
보호되어 브라우저가 직접 다루면 된다. 서버가 세션을 알 필요가 없으니 세션 쿠키도, 토큰
갱신용 proxy도 필요 없다. Next 16 문서도 proxy를 "다른 방법이 없을 때의 최후 수단"이라고
못박는다. 방 데이터는 익명 참가자 때문에 여전히 서버 라우트 + secret 키로 간다.

### 게스트 → 로그인 병합

로그인은 **선택이다.** 막는 기능이 하나도 없고, 안 눌러도 전부 그대로 돌아간다.
로그인 시 로컬에 쌓인 것과 계정에 있는 것을 합친다.

| | 합치는 법 | 이유 |
|---|---|---|
| 못 먹는 것 | **합집합** | "해산물 못 먹음"은 기기의 속성이 아니라 사실이다 |
| 취향 카운트 | **더하기** | 좋아요 3번 + 2번 = 5번. 같은 사람의 평가다 |

어느 한쪽으로 덮어쓰지 않는다. 로그인했다고 그동안 쌓은 게 날아가도, 다른 기기에서 쌓은
것을 잃어도 안 된다.

## 구현 시 반복해서 틀리는 지점

- **좌표 순서** — 카카오 API는 `x`가 경도, `y`가 위도다. 브라우저 Geolocation은 `coords.latitude`/`coords.longitude`. 그리고 지도 SDK의 `new kakao.maps.LatLng(위도, 경도)`는 또 순서가 반대다. 뒤집으면 마커가 서해에 찍힌다. `src/lib/params.ts`가 한반도 범위를 벗어난 좌표를 400으로 걷어낸다.
- **위치 권한 거부 폴백** — `navigator.geolocation`은 보안 컨텍스트에서만 동작한다(`localhost` 예외, 배포는 Vercel HTTPS). 거부는 흔하므로 **"지역 직접 선택" 폴백**(`src/lib/regions.ts`)이 처음부터 있다. 지우지 말 것.
- **Maps SDK는 `autoload=false`** 로 불러오고 반드시 `kakao.maps.load()` 콜백 안에서만 지도를 만든다. 스크립트가 붙자마자 만들면 터진다.
- **조건 완화를 조용히 하지 않는다** — 후보가 0개라 반경이나 필터를 풀었으면 무엇을 풀었는지 응답(`relaxed`)에 담아 UI에 표시한다. 조용히 넓히면 사용자는 자기 조건이 지켜진 줄 안다.
- **추천 로직에 LLM을 넣지 않는다** — 룰 기반 가중 랜덤으로 체감 품질이 충분하고 응답이 즉시다. 대화형 요구가 실제로 생길 때 검토한다.
- **HTTP 클라이언트로 axios를 추가하지 않는다** — 금지가 아니라 현재 필요가 없다는 뜻이다. 우리가 쓰는 건 카카오 GET 두 개뿐이고, fetch에 없는 두 가지(4xx/5xx에서 throw하지 않음, 타임아웃 없음)는 `src/lib/kakao/client.ts`가 `res.ok` 검사와 `AbortSignal.timeout()`으로 이미 메웠다. 게다가 Next의 fetch 캐싱(`next: { revalidate }`)은 axios로는 못 쓴다. **업로드 진행률**(fetch로 불가), 토큰 갱신 인터셉터, 엔드포인트가 수십 개로 늘어나는 상황이 오면 그때 다시 판단한다.

## 명령어

```bash
npm run dev          # 개발 서버 (Turbopack)
npm run build        # 프로덕션 빌드
npm run lint         # ESLint
npm run verify:seed  # 시드 메뉴가 원칙 1을 지키는지 검사 (카카오 호출)
```

## 구조

```
src/app/page.tsx            안내 화면 (시작하기 · 같이 고르기 · 로그인)
src/app/recommend/          혼자 추천 화면
src/app/room/               방 만들기 / 코드로 입장
src/app/room/[code]/        방 화면
src/app/mokrog/             나의 먹로그 (기록 카드 + 배지)
src/app/api/reviews/        후기 쓰기·별점 요약·가게별 후기·내 기록·지우기·로그인 시 계정 연결
src/app/api/candidates/     주변 업종 → 메뉴 후보 역산 (카카오 카테고리 검색)
src/app/api/places/         메뉴명 → 가게 목록 (카카오 키워드 검색)
src/app/api/geocode/        좌표 → 주소 (지도에서 찍은 곳 표시용)
src/app/api/search-location/ 장소 이름 → 좌표 (위치 고르기의 검색창. 업종 없이 역·건물도 찾는다)
src/app/api/rooms/          방 생성·입장·조건 제출·투표·마감
src/app/api/auth/kakao/     카카오 로그인 start(인가 화면으로) / callback(id_token 교환)
src/lib/kakao/              카카오 API 계층. client.ts·oidc.ts는 server-only
                            relevance.ts — 느슨한 키워드 결과에서 실제로 파는 집만 추림
src/lib/menu/               시드 데이터, 업종 매핑, 영업시간 추정 테이블
src/lib/preference/         취향 기록(localStorage) + 학습 점수
src/lib/profile/            못 먹는 것(localStorage) + 로그인 계정 동기화
src/lib/room/               방 도메인 — 조건 합치기, 서비스 로직
src/lib/review/             후기 도메인 — 서비스(server-only), 브라우저 호출, 배지 계산
src/lib/supabase/           client.ts — PostgREST 얇은 래퍼. server-only
                            browser.ts — 로그인·내 프로필용 supabase-js 클라이언트
supabase/migrations/        DB 스키마 (대시보드에서 수동 실행)
src/lib/recommend.ts        후보 도출, 필터 완화, 가중 랜덤
src/hooks/                  useGeolocation, useRecommendation, useRoom, useProfile, useAuth
src/components/             OptionPanel, MenuCard, PlaceList, PlaceMap, LocationPicker, AuthBar, Brand
scripts/verify-seed.ts      원칙 1 검사
```

## 참고

- 패키지 매니저는 **npm**. (레포명 `BYPP`는 대문자라 npm 패키지명 규칙에 걸리므로 `package.json`의 `name`은 `bypp`로 유지한다.)
- 스크립트는 Node 24의 네이티브 TypeScript 실행을 쓴다. 별도 러너(tsx 등)를 추가하지 않는다. 그래서 `tsconfig.json`에 `allowImportingTsExtensions`가 켜져 있다.
- `AGENTS.md`의 Next.js 규칙 블록은 `next dev`가 자동 생성/갱신하므로 직접 지우지 않는다.
