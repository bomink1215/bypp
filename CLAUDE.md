@AGENTS.md

# BYPP

사용자의 현재 위치를 기반으로 **오늘 먹을 메뉴를 추천**하고, 그 메뉴를 파는 주변 음식점을 찾아주는 웹 서비스.

## 핵심 설계 결정: 추천 방향을 뒤집는다

이 프로젝트의 진짜 제약은 프레임워크가 아니라 **메뉴 데이터의 부재**다. 카카오/네이버/구글 어떤 공개 API도 가게의 메뉴판을 제공하지 않고, 포털 크롤링은 각 사이트 약관 위반 소지가 있어 채택하지 않는다.

그래서 흐름을 역방향으로 설계한다.

- ❌ 주변 가게 수집 → 가게별 메뉴 수집 → 추천  *(메뉴 데이터가 없어 막힘)*
- ✅ **메뉴를 먼저 추천** → 그 메뉴명 + 사용자 좌표로 카카오 **키워드 검색** → 근처에서 파는 가게 목록 표시

메뉴 후보(한식/양식/야식/해장/가벼운 것 등 50~100개)는 저장소 안에 직접 관리하는 시드 데이터로 둔다. 이 구조 덕분에 외부 데이터 의존이 사라지고 추천 로직 자체에 집중할 수 있다.

**이 방향을 바꾸는 제안(가게 먼저 나열 후 메뉴 표시 등)은 메뉴 데이터 출처 문제를 먼저 해결해야 한다.**

## 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js 16 (App Router) + TypeScript | API 키를 숨길 서버 라우트가 프론트와 한 프로젝트에 존재 |
| 스타일 | Tailwind CSS v4 | 모바일 우선 UI를 빠르게 |
| 장소 검색 | 카카오 로컬 API (REST) | 좌표+반경 검색, 음식점 카테고리 코드 `FD6` / 카페 `CE7`, 무료 쿼터 넉넉 |
| 지도 | 카카오 Maps JS SDK | 로컬 API와 동일 생태계 |
| 배포 | Vercel | HTTPS 자동 제공(Geolocation 필수 조건), GitHub push 시 자동 배포 |
| DB | 당분간 없음 | 즐겨찾기·추천 기록이 필요해지면 그때 Supabase(Postgres) 도입 |

검토 후 제외한 것:
- **네이버 지역검색 API** — 한 번에 최대 5건만 반환해 목록 서비스에 부적합.
- **Google Places** — 결제수단 등록 필수, 국내 POI 품질이 카카오보다 낮음.
- **Vite + React** — API 키를 가릴 백엔드를 따로 띄워야 함.

## 환경변수 규칙 (중요)

`.env.example`이 필요한 키의 목록이다. 실제 값은 `.env.local`에 넣고, 이 파일은 커밋하지 않는다.

```
KAKAO_REST_API_KEY=        # 서버 전용
NEXT_PUBLIC_KAKAO_JS_KEY=  # 브라우저 노출됨
```

- `NEXT_PUBLIC_` 접두사가 붙은 값은 **브라우저 번들에 그대로 박힌다.**
- 지도 SDK용 JS 키는 카카오 콘솔에서 도메인 제한이 걸리므로 노출돼도 된다.
- **REST 키에는 절대 `NEXT_PUBLIC_`을 붙이지 않는다.** 카카오 로컬 API 호출은 반드시 서버 라우트(`src/app/api/**/route.ts`)에서 수행하고, 클라이언트는 그 라우트만 호출한다.

## 구현 시 반복해서 틀리는 지점

- **좌표 순서** — 카카오 API는 `x`가 경도(longitude), `y`가 위도(latitude)다. 브라우저 Geolocation은 `coords.latitude` / `coords.longitude`. 이 둘을 바꿔 넣는 실수가 가장 흔하다.
- **위치 권한 거부 폴백** — `navigator.geolocation`은 보안 컨텍스트에서만 동작한다(`localhost`는 예외로 허용, 배포는 Vercel HTTPS). 사용자가 권한을 거부하는 경우가 흔하므로 **"지역 직접 선택" 폴백 UI를 처음부터 넣는다.**
- **HTTP 클라이언트** — axios를 추가하지 않는다. Node 24 / Next 내장 `fetch`로 충분하다.
- **추천 로직에 LLM을 넣지 않는다** — 시간대 + 날씨 + 최근 선택 제외 정도의 룰 기반 가중 랜덤으로 체감 품질이 충분하고 응답도 즉시다. "어제 매운 걸 먹었으니 오늘은 순한 걸로" 같은 대화형 요구가 실제로 생길 때 검토한다.

## 명령어

```bash
npm run dev     # 개발 서버 (Turbopack)
npm run build   # 프로덕션 빌드
npm run lint    # ESLint
```

## 구조

```
src/app/            App Router 페이지
src/app/api/        서버 라우트 — 카카오 REST 호출은 전부 여기서
```

## 참고

- 패키지 매니저는 **npm**. (레포명 `BYPP`는 대문자라 npm 패키지명 규칙에 걸리므로 `package.json`의 `name`은 `bypp`로 유지한다.)
- `AGENTS.md`의 Next.js 규칙 블록은 `next dev`가 자동 생성/갱신하므로 직접 지우지 않는다.
